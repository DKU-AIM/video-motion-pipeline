# Core versions used in the successful A100 MIG / Python 3.10 run.
# Install torch/torchvision from the cu118 index first (bootstrap_elice.sh).
torch==2.6.0
torchvision==0.21.0
transformers==4.57.1
accelerate==1.6.0
qwen-vl-utils==0.0.14
av
matplotlib
